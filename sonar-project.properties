# Exclude Postman collections, test resources, and generated HTML reports from SonarCloud analysis
# to prevent Quality Gate failures due to code duplication in test scripts and JSON mock data
sonar.exclusions=test/resources/**,reporters/**,backend/target/**,**/node_modules/**
